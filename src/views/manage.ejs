<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link rel="stylesheet" href="css/main.css">
    <link rel="stylesheet" href="css/s_css.css">
</head>

<body>
    <%- include('nav.ejs'); %>
    <div class="board_wrap">
        <div class="board_title">
            <strong>상품 관리</strong>
            <p>등록된 상품목록</p>
        </div>
        <div class="board_list_wrap">
            <div class="board_list">
                <div class="top">
                    <div class="image">상품 이미지</div>
                    <div class="title">제목</div>
                    <div class="price">가격</div>
                    <div class="date">유통기한</div>
                    <div class="state">상태</div>
                </div>
                <% for(var i = 0; i < items.length; i++) { %>
                <div>
                    <div class="image"><img src="<%= items[i].i_image %>" style="width: 5vw; min-width: 40px;"></div>
                    <div class="title">
                        <%= items[i].i_name %>
                    </div>
                    <div class="price">
                        <div class="p1">가격 :
                            <%= items[i].i_price %>
                        </div>
                        <div class="dis_price">할인가 :
                            <%= items[i].i_now_price %>
                        </div>
                    </div>
                    <div class="date"><%= items[i].i_exp %></div>
                    <div class="state">
                        <div><%= items[i].i_stauts %></div>
                        <select id="pick" class="sel">
                            <%
                                var options = ["판매 대기", "판매 중", "판매 완료", "기타"];
                                for(var j = 0; j < options.length; j++) {
                                    var selected = (items[i].i_status == j) ? "selected" : "";
                            %>
                            <option value="<%= j %>" <%= selected %>><%= options[j] %></option>
                            <%
                                }
                            %>
                        </select>
                    </div>
                </div>
                <% } %>
            </div>
            <div class="board_page">
                <a href="#" class="bt prev">
                    <</a> <a href="#" class="num on">1
                </a>
                <a href="#" class="num">2</a>
                <a href="#" class="num">3</a>
                <a href="#" class="num">4</a>
                <a href="#" class="num">5</a>
                <a href="#" class="bt next">></a>
            </div>
            <div class="bt_wrap">
                <a href="manage.html" class="on">저장</a>
            </div>
        </div>
    </div>
</body>

</html>